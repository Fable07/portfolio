<!--
  App — root component. It only renders the matched route; the page frame
  (sidebar, admin header…) comes from the layout each route uses.
  See src/router/index.js for the route → layout → view mapping.
-->
<template>
  <RouterView />
</template>
